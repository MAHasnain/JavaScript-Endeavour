// Immediately execute hony wala function

// Immediately invoked function expression


((name) => {
    console.log(`DB connected ${name}`);
})("MAH");

// (function addTwo (num1, num2) {          named IIFE
//     num1 + num2
// }) (12, 12);
